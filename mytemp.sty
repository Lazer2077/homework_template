% mytemplate.sty

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{mytemp}[2023/09/27 My LaTeX Template]
\RequirePackage{float}
\RequirePackage[verbose=true,letterpaper]{geometry}
\RequirePackage{longtable}
\RequirePackage{multirow}
\RequirePackage{diagbox}
\RequirePackage[figuresright]{rotating}
\RequirePackage{makecell}
\RequirePackage{amsmath, amsthm, amssymb, bm, graphicx, hyperref, mathrsfs}
\RequirePackage{listings}
\RequirePackage{pythonhighlight}
\RequirePackage{appendix}
\RequirePackage{subfigure}
\RequirePackage{epstopdf}
\RequirePackage{booktabs}       % professional-quality tables
\RequirePackage{amsfonts}       % blackboard math symbols
\RequirePackage{nicefrac}       % compact symbols for 1/2, etc.
\RequirePackage{microtype}      % microtypography
\RequirePackage{titlesec}
\RequirePackage{mfirstuc}
\RequirePackage[ruled,linesnumbered]{algorithm2e}
\geometry{a4paper,left=1.5cm,right=1.5cm,top=1cm,bottom=1.5cm}
\titleformat{\section}
{\normalfont\Large\bfseries}{\thesection}{1em}{\capitalisewords}

\lstset{
	language = matlab,
	backgroundcolor = \color{yellow!10}, % 背景色：淡黄
	basicstyle = \small\ttfamily, % 基本样式 + 小号字体
	rulesepcolor= \color{gray}, % 代码块边框颜色
	breaklines = true, % 代码过长则换行
	numbers = left, % 行号在左侧显示
	numberstyle = \small, % 行号字体
	keywordstyle = \color{blue}, % 关键字颜色
	commentstyle =\color{green!100}, % 注释颜色
	stringstyle = \color{red!100}, % 字符串颜色
	frame = shadowbox, % 用(带影子效果)方框框住代码块
	showspaces = false, % 不显示空格
	columns = fixed, % 字间距固定
	%escapeinside={} % 特殊自定分隔符：
	morekeywords = {as}, % 自加新的关键字(必须前后都是空格)
	deletendkeywords = {compile} % 删除内定关键字；删除错误标记的关键字用deletekeywords删！
}
\widowpenalty=10000
\clubpenalty=10000
\flushbottom
\sloppy
\linespread{1.15}
\newcounter{problemname}
\newenvironment{case}{\stepcounter{problemname}\par\noindent\textsc{\textbf{Question} \arabic{problemname}. }}{\\\par}
\newenvironment{solution}{\par\noindent\textsc{Solution. }}{\\\par }
\newenvironment{note}{\par\noindent\textsc{Note of Problem \arabic{problemname}. }}{\\\par}
\endinput
\usepackage{color}
\usepackage{enumitem}
\usepackage[final]{pdfpages}